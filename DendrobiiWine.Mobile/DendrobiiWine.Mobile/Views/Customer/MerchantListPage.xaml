<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DendrobiiWine.Mobile.Views.Customer.MerchantListPage"
             Title="商户列表">
  <ContentPage.ToolbarItems>
    <ToolbarItem Name="无锡" Activated="OnCityActivated"/>
    <ToolbarItem Name="二维码" Activated="OnBarcodeActivated" />
  </ContentPage.ToolbarItems>
  <ContentPage.Content>
    <ListView x:Name="listView" ItemSelected="OnItemSelected" IsPullToRefreshEnabled="true">
      <ListView.ItemTemplate>
        <DataTemplate>
          <!--<ViewCell>
            <StackLayout Padding="20,0,0,0" HorizontalOptions="StartAndExpand" Orientation="Horizontal">
              <Label Text="{Binding Name}" YAlign="Center" />
            </StackLayout>
          </ViewCell>-->
          <TextCell Text="{Binding DisplayName}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </ContentPage.Content>
</ContentPage>